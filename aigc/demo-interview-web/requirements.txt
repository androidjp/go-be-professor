--------------
我希望当前目录下生成一个golang后端项目，此项目暴露一个http服务，监听在8222端口。
2. golang后端项目提供两个内部接口：
  - 第一个内部接口 “/api/dev/v1/pdf/extract”：入参是某个前端页面上传的pdf文档，接口逻辑是通过开源pdf文档内容提取库，最终返回 结构化的用户简历相关数据信息。
  - 第二格内部接口 “/api/dev/v1/userinfo/summary”：入参是pdf提取到的用户相关的结构化信息，通过调用 本地ollama API让大模型总结得到此文本的精炼信息，并最终返回 精炼后的用户上下文信息。
3. golang后端项目提供一个对端Server-Sent-Event接口协议 “/api/v1/interview/info/get_all”: 入参是 用户简历上下文精炼信息 以及 前端传入的 面试要求信息， 主要逻辑是 将入参整理为 一个合理的提示词内容，并调用本地的 Ollama API，最终响应是 面试问题 和 答案的列表，给到前端渲染。


